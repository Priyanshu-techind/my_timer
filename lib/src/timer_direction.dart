/// Direction in which a [MyTimer] runs.
enum TimerDirection {
  /// Counts up from zero toward the configured duration.
  countUp,

  /// Counts down from the configured duration toward zero.
  countDown,
}
